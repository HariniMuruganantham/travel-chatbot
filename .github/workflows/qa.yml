name: QA
on: [push, pull_request]

jobs:
  detect:
    uses: HariniMuruganantham/Automated-qa-pipeline/.github/workflows/detect-stack.yml@main
    with:
      target_repo: ${{ github.repository }}
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}